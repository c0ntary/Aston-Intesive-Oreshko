spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}

  mail:
    host: localhost
    port: 3025
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

eureka:
  client:
    enabled: false